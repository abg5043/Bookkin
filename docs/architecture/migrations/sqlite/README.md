# Archived SQLite migration history

These migrations describe the historical local SQLite implementation through Checkpoint 5A. They are retained unchanged for audit and are not an active Prisma migration history.

Checkpoint 5B establishes a fresh PostgreSQL-specific baseline under `prisma/migrations`. Do not copy, replay, or modify these SQLite statements for PostgreSQL.

