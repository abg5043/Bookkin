# Stories and issues

Stories should describe a small user outcome, its observable acceptance criteria, and the checkpoint that owns it.

Do not create implementation work for later checkpoints before the current checkpoint is approved.
