# Checkpoint 2 uses a local SQLite database. This value is not a secret.
DATABASE_URL="file:./dev.db"

# Checkpoint 1 does not require external services or secrets.
# Keep this file safe to commit. Add future variables here with documentation.
NEXT_PUBLIC_APP_URL=http://localhost:3000
